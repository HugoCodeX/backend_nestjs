# Auth Service environment variables

# HTTP server port
PORT=3001

# gRPC server port
GRPC_PORT=5001

# JWT signing/verification secret
JWT_SECRET=replace-with-a-long-random-secret

# Database connection (Neon/Drizzle) — local Docker: auth-postgres
DATABASE_URL=postgres://auth:auth@localhost:5433/auth_db

