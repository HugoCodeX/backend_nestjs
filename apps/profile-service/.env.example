# Profile Service environment variables

# HTTP server port
PORT=3002

# Database connection (Neon/Drizzle) — local Docker: profile-postgres
DATABASE_URL=postgres://profile:profile@localhost:5434/profile_db

# Where profile-service reaches auth-service (gRPC)
AUTH_SERVICE_GRPC_URL=localhost:5001

